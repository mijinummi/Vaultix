import { http, HttpResponse } from 'msw';

export const handlers = [
  // Example handler for future tests
  // http.get('/api/escrows', () => {
  //   return HttpResponse.json([{ id: '1', status: 'pending' }]);
  // }),
];
